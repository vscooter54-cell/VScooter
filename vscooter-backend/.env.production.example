# PRODUCTION ENVIRONMENT CONFIGURATION
# ========================================
# IMPORTANT: Never commit this file with actual secrets!
# Copy to .env.production and fill in the values

# MongoDB Configuration
# Use MongoDB Atlas or dedicated MongoDB server
MONGODB_URI=mongodb+srv://prod_user:STRONG_PASSWORD@production-cluster.mongodb.net/vscooter_production?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration
# CRITICAL: Generate strong secrets with: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=GENERATE_STRONG_256BIT_SECRET_HERE
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=GENERATE_DIFFERENT_STRONG_SECRET_HERE
JWT_REFRESH_EXPIRE=30d

# Email Configuration
# PRODUCTION: Use SendGrid, AWS SES, Mailgun, or similar service
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USER=apikey
EMAIL_PASSWORD=YOUR_SENDGRID_API_KEY
FROM_NAME=VScooter
FROM_EMAIL=noreply@vscooter.com
JWT_COOKIE_EXPIRE=7

# Stripe Configuration - PRODUCTION KEYS
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_YOUR_PRODUCTION_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_PRODUCTION_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_PRODUCTION_WEBHOOK_SECRET

# Frontend URL (for CORS)
# Set to your production domain
FRONTEND_URL=https://www.vscooter.com

# Admin Configuration
# CRITICAL: Use a very strong password (min 20 characters)
ADMIN_EMAIL=admin@vscooter.com
ADMIN_PASSWORD=USE_VERY_STRONG_PASSWORD_HERE_MIN_20_CHARS

# Security Notes for Production:
# 1. All secrets must be 256-bit or stronger
# 2. Use environment variable management (AWS Secrets Manager, Azure Key Vault, etc.)
# 3. Enable SSL/HTTPS only
# 4. Configure firewall to allow only necessary ports
# 5. Set up MongoDB IP whitelist
# 6. Enable MongoDB authentication and encryption at rest
# 7. Configure rate limiting for production load
# 8. Set up monitoring and alerting
